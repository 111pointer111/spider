# spider

spider is an Obsidian plugin for building AI conversations as an expandable knowledge map.

When you are reading an AI answer and meet an unfamiliar concept, press `Tab` to create a child chat node. You can keep pressing `Tab` inside child nodes to drill down without a depth limit. The graph stays in a full Obsidian tab, while the current node chat lives in the right sidebar like a normal Obsidian plugin panel.

## Features

- Full-tab knowledge graph powered by React Flow.
- Right-sidebar chat panel for the currently selected node.
- Infinite parent-child chat branches.
- `Tab` creates a child node.
- `Shift + Tab` returns to the parent node.
- Optional child node creation from selected text.
- OpenAI-compatible API settings.
- Streaming AI responses.
- AI-generated node and map titles, editable from the chat panel.
- Chinese and English UI switch.
- Local vault storage in `.spider/maps`.
- Existing maps from older `.branch-chat-map/maps` installs are still readable.
- Structured export packages with Markdown, Mermaid mindmap, Obsidian Canvas, and raw JSON.

## Interaction Model

- Click the ribbon icon or run `Open spider`.
- The knowledge graph opens in a normal main editor tab.
- The node chat opens as a right-sidebar tab with the plugin icon.
- Click a node in the graph to switch the active chat.
- Press `Tab` in the graph or chat panel to create a child node.
- Press `Shift + Tab` to move back to the parent node.
- Press `Enter` in the composer to send, and `Shift + Enter` for a newline.

## Exports

Each root map exports into its own folder under the configured export directory:

```text
Spider Maps/
  2026-06-22-1430-ai-learning/
    README.md
    nodes/
      01-root-question.md
      02-child-question.md
    diagrams/
      mindmap.mermaid.md
    canvas/
      map.canvas
    data/
      map.json
```

`README.md` is the reading entry point, with overview metadata, a recommended reading path, and a Mermaid preview. Node files contain summaries, anchors, parent/child links, and the full chat history.

## Privacy

The plugin does not read your whole vault by default.

AI requests include:

- current node messages
- optional parent title, parent summary, and selected anchor text
- configured model and endpoint metadata needed for the request

Your API key is stored in Obsidian plugin data on your device.

## Installation For Development

```bash
npm install
npm run build
```

Then copy these files into your Obsidian vault:

```text
.obsidian/plugins/spider/
  main.js
  manifest.json
  styles.css
```

Reload Obsidian or disable and re-enable the plugin.

## Development

```bash
npm install
npm run dev
```

Useful commands:

```bash
npm run build
npm test
npm audit --omit=dev
```

## Project Structure

```text
src/
  ai/          OpenAI-compatible API provider
  domain/      chat map mutations, guards, layout
  export/      Markdown, Mermaid, and Canvas exporters
  state/       shared Obsidian view state store
  storage/     vault-backed map persistence
  ui/          React views and components
  utils/       small ID and text helpers
```

## Release Assets

Obsidian release assets are generated by:

```bash
npm run build
```

The generated files are:

- `main.js`
- `manifest.json`
- `styles.css`

## License

MIT
